namespace Quizou.Domain.Enums
{
    public enum QuizStatus
    {
        Draft,
        Active,
        Deleted,
        Archived
    }

    public enum Difficulty
    {
        Easy,
        Medium,
        Hard
    }

    public enum Roles
    {
        Standard,
        Admin
    }
}